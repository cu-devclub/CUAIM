#Run Singularity with Srun directly
#Choose QOS in terminal
srun \
  -p gpu \
  -c 2 \
  --mem=8G \
  --qos=gpu40g \
  --gres=gpu:7g.40gb:1 \
  --time=02:00:00 \
  --pty bash -l


#Define Paths 

#Do this every time you have to open new session

PROJECT=/data/project/your_username/your_project
IMAGE=$PROJECT/containers/your_container.sif
ENV_TAG=your_environment
VENV=/workspace/.venv-$ENV_TAG

singularity exec --nv \
  --pwd /workspace \
  --bind "$PROJECT:/workspace:rw" \
  "$IMAGE" \
  "$VENV/bin/python" \
  /workspace/script1.py